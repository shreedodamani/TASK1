# Django Settings
SECRET_KEY=django-insecure-unique-development-key-for-hms-project-1875
DEBUG=True
ALLOWED_HOSTS=*

# Database Settings (Leave blank to use local SQLite)
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432

# Google Calendar OAuth2 Credentials (Leave blank to use Mock Mode)
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
GOOGLE_OAUTH_REDIRECT_URI=http://127.0.0.1:8000/oauth2/callback/

# Email Notification Service Settings
EMAIL_SERVICE_URL=http://127.0.0.1:3000/dev/send-email

# SMTP Configuration (Leave blank to use local mock_smtp.py console server)
SMTP_HOST=127.0.0.1
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=
SENDER_EMAIL=noreply@minihms.com
